from setuptools import setup, find_packages

setup(
    name="myscriptslib",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        # Add any dependencies here, if you have any.
    ],
)
